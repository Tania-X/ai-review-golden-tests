# 项目约定(golden 测试仓库)

本文件定义本仓库的代码约定, 既是 AI 审查的上下文依据, 也是 `case-convention` 场景的判定标准。

> 评测机制说明(防剧透/答案分离等)见 `docs/golden-contract.md`, 不属于代码约定。

## 错误处理

- 所有返回 error 的调用, error **必须被处理**(返回/记录/有注释说明的显式忽略)。
- **禁止**无注释地吞掉 error(如裸 `_ = fn()` 或调用后完全忽略返回值)。

## 命名与结构

- 导出符号(大写开头)**必须**有注释说明用途。
- 函数保持单一职责, 避免超长函数。

## 安全

- 禁止 SQL 字符串拼接、硬编码密钥/密码。
- 外部输入(用户/上游)一律视为不可信。
